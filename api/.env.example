# Copy to api/.env and fill in real values. Never commit .env (gitignored).

# Neo4j password — must match the password your local Neo4j was started with.
# Example Docker run:
#   docker run -p 7474:7474 -p 7687:7687 -e NEO4J_AUTH=neo4j/$NEO4J_PASSWORD neo4j:5
NEO4J_PASSWORD=changeme